import React from 'react';
import LoginButton from '../component/LoginButton';

const LoginPage = () => {
  return (
    <div>
      <h2>Login</h2>
      <LoginButton />
    </div>
  );
};

export default LoginPage;